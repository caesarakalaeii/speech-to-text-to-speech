# StyleTTS2 dependencies
# Install these if you want to use StyleTTS2 with voice cloning
# Installation: pip install -r requirements-styletts2.txt

# PyTorch (install BEFORE this file for proper CUDA/CPU selection)
# IMPORTANT: Install PyTorch 2.4.1 first using ONE of these commands:
#   CPU: pip install torch==2.4.1 torchaudio==2.4.1 --extra-index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1: pip install torch==2.4.1 torchaudio==2.4.1 --extra-index-url https://download.pytorch.org/whl/cu121
# Then install this requirements file: pip install -r requirements-styletts2.txt

# Base dependencies (required for all TTS services)
openai-whisper==20231117
pyaudio>=0.2.13
python-dotenv>=1.0.0
websockets>=12.0

# NumPy 1.x required for compatibility with PyTorch 2.4.1
numpy>=1.24.0,<2.0.0

# StyleTTS2 - Human-level TTS with voice cloning
# MIT licensed, supports voice cloning from short samples
styletts2>=0.8.0

# Note: StyleTTS2 automatically downloads pre-trained models from HuggingFace
# Voice cloning works with just a reference audio file (3-15 seconds)
# No system dependencies like espeak-ng required
