# Parakeet TDT dependencies
# Install these if you want to use Parakeet TDT instead of Whisper for speech-to-text
# Installation: pip install -r requirements-parakeet.txt

# PyTorch (install BEFORE this file for proper CUDA/CPU selection)
# IMPORTANT: Install PyTorch 2.4.1 first using ONE of these commands:
#   CPU: pip install torch==2.4.1 torchaudio==2.4.1 --extra-index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1: pip install torch==2.4.1 torchaudio==2.4.1 --extra-index-url https://download.pytorch.org/whl/cu121
# Then install this requirements file: pip install -r requirements-parakeet.txt

# Base dependencies (required for all services)
pyaudio>=0.2.13
python-dotenv>=1.0.0
websockets>=12.0

# NumPy 1.x required for compatibility with PyTorch 2.4.1
numpy>=1.24.0,<2.0.0

# NeMo toolkit with ASR support for Parakeet models
# Includes all necessary dependencies for running ASR models
nemo_toolkit[asr]>=2.0.0

# Note: NeMo will automatically download the Parakeet model on first use
# The model will be cached in ~/.cache/huggingface/hub/
# Parakeet-TDT-0.6b-v3 is ~600MB download
