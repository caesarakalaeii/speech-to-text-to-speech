# ============================================================================
# TTS Service Selection
# ============================================================================
# Choose ONE: speakerbot, neutts, piper, or styletts2
TTS_SERVICE=speakerbot

# ============================================================================
# Speakerbot Settings (TTS_SERVICE=speakerbot)
# ============================================================================
# External WebSocket TTS service
SPEAKERBOT_WEBSOCKET_URL=ws://localhost:8080
VOICE_NAME=Sally

# ============================================================================
# NeuTTS Air Settings (TTS_SERVICE=neutts)
# ============================================================================
# Local neural TTS with voice cloning (requires PyTorch, espeak-ng)
# Install: pip install -r requirements-neutts.txt
# Backbone model options: neuphonic/neutts-air, neuphonic/neutts-air-q4-gguf, neuphonic/neutts-air-q8-gguf
NEUTTS_BACKBONE=neuphonic/neutts-air-q4-gguf
NEUTTS_BACKBONE_DEVICE=cpu
NEUTTS_CODEC=neuphonic/neucodec
NEUTTS_CODEC_DEVICE=cpu
# Reference audio for voice cloning (3-15 seconds, mono, 16-44kHz, .wav format)
NEUTTS_REF_AUDIO=samples/reference.wav
# Transcription of reference audio
NEUTTS_REF_TEXT=samples/reference.txt

# ============================================================================
# Piper TTS Settings (TTS_SERVICE=piper)
# ============================================================================
# Fast local TTS with ONNX models (no voice cloning, pre-trained voices only)
# Install: pip install -r requirements-piper.txt
# Download voices from: https://huggingface.co/rhasspy/piper-voices
# Example: PIPER_VOICE_PATH=voices/en_US-amy-medium.onnx
PIPER_VOICE_PATH=

# ============================================================================
# StyleTTS2 Settings (TTS_SERVICE=styletts2)
# ============================================================================
# Modern neural TTS with voice cloning (requires PyTorch)
# Install: pip install -r requirements-styletts2.txt
# Reference audio for voice cloning (3-15 seconds, any format)
# Leave empty to use default voice
STYLETTS2_REF_AUDIO=samples/reference.wav

# Whisper model size: tiny, base, small, medium, large
# Larger models are more accurate but slower
WHISPER_MODEL=base

# Audio settings
SAMPLE_RATE=16000
CHUNK_DURATION=3.0

# Speech detection threshold (0.0 to 1.0)
# Lower values detect quieter speech
SILENCE_THRESHOLD=0.01

# Minimum speech duration in seconds
MIN_SPEECH_DURATION=0.5
